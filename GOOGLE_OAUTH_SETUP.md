# تعليمات إعداد Google OAuth

اتبع هذه الخطوات للحصول على Google OAuth Client ID:

## الخطوة 1: الذهاب إلى Google Cloud Console
افتح الرابط: https://console.cloud.google.com/

## الخطوة 2: إنشاء أو اختيار المشروع
- إذا كان لديك مشروع موجود مسبقاً، اختره
- وإلا، اضغط على **Create Project** (إنشاء مشروع) وسمّه (مثلاً: "Teacher Report App")

## الخطوة 3: إنشاء بيانات اعتماد OAuth
1. اذهب إلى **APIs & Services** > **Credentials**
2. اضغط **Create Credentials** > **OAuth client ID**
3. إذا طُلب منك، قم بتكوين شاشة الموافقة:
   - **User Type**: اختر **External** (خارجي)
   - **App name**: "تقارير الحصص" أو "Teacher Report App"
   - **User support email**: بريدك الإلكتروني
   - **Developer contact**: بريدك الإلكتروني
   - اضغط **Save and Continue** في جميع الخطوات

## الخطوة 4: إعداد OAuth Client
1. **Application type**: اختر **Web application** (تطبيق ويب)
2. **Name**: "Teacher Report Web Client"
3. **Authorized JavaScript origins** (أصول JavaScript المصرح بها):
   - `http://localhost:3000` (للتطوير المحلي)
   - `https://teacher-s-report.vercel.app` (للإنتاج)
4. **Authorized redirect URIs**: (اتركه فارغاً في الوقت الحالي)
5. اضغط **Create** (إنشاء)

## الخطوة 5: نسخ Client ID
- ستظهر نافذة منبثقة تحتوي على **Client ID**
- انسخ هذا الرقم (يبدو مثل: `123456789-abc...xyz.apps.googleusercontent.com`)
- احفظه في مكان آمن

## الخطوة 6: إضافته إلى .env.local
أضف هذا السطر إلى ملف `.env.local`:
```
VITE_GOOGLE_CLIENT_ID=your-client-id-here
```

## ملاحظات مهمة
- ⚠️ احتفظ بـ Client ID بشكل آمن (رغم أنه ليس حساساً مثل المفتاح السري)
- 🌐 للإنتاج، تأكد من إضافة نطاق Vercel الخاص بك إلى Authorized JavaScript origins
- 🔄 بعد إضافة Client ID، أعد تشغيل السيرفر المحلي (`npm run dev`)

